name: Lint
on: 
  push:
    paths:
      - popup/*
jobs: 
  main:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: npm i -g eslint
    - name: Lint files
      run: eslint popup --ext .js